const KEY = "pinntorp_state_v1";

export function loadState() {
    // this function will load the state from localStorage and return it as a JavaScript object, or return the default state if there is no saved state in localStorage
}

export function saveState(state) {
    // this function will save the state to localStorage as a JSON string
}