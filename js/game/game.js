import { state } from "../core/state.js";
import { saveState } from "../core/storage.js";

export function playRound(guess) {
    // this function will play a round of a test game (preferably small) and update the state accordingly by incrementing the wins, losses, and gamesPlayed properties in the stats object based on whether the guess was correct or not, and then save the updated state to localStorage
}