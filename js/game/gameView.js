import { playRound } from "./game.js";
import { renderStats } from "../stats/statsView.js";

export function initGameView() {
    // this function will initialize the game view and set up event listeners for the game controls such as a button to submit a guess that will call the playRound function with the appropriate guess value when clicked, and then call the renderStats function to update the stats view after each round is played
}